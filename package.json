{
  "name": "vulture",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter @vulture/desktop-ui dev",
    "test": "bun test packages/protocol/src",
    "protocol:openapi": "bun --filter @vulture/protocol openapi",
    "protocol:openapi:check": "bun --filter @vulture/protocol openapi:check",
    "harness:acceptance": "bun --filter @vulture/gateway harness:acceptance",
    "harness:runtime": "bun --filter @vulture/gateway harness:runtime",
    "harness:tools": "bun --filter @vulture/gateway harness:tools",
    "harness:ui-smoke": "bun run --cwd apps/desktop-ui harness:ui-smoke",
    "harness:desktop-e2e": "bun --filter @vulture/desktop-e2e desktop-e2e",
    "harness:catalog": "bun scripts/harnessCatalog.ts",
    "harness:doctor": "bun scripts/harnessDoctor.ts",
    "harness:report": "bun scripts/harnessReport.ts",
    "harness:ci": "bun scripts/harnessCi.ts",
    "typecheck": "bun --filter '*' typecheck",
    "build": "bun --filter '*' build",
    "verify:browser": "node -e \"JSON.parse(require('fs').readFileSync('extensions/browser/manifest.json','utf8')); console.log('manifest ok')\" && node --check extensions/browser/src/background.js && node --check extensions/browser/src/content.js && node --check extensions/browser/src/popup.js && node --check extensions/browser/src/relay-client.js && bun --filter '*' typecheck && cargo test -p vulture-tool-gateway browser && cargo test -p vulture-desktop-shell browser && cargo test -p vulture-desktop-shell audits_browser_tool_requests_through_policy && cargo clippy --workspace -- -D warnings",
    "verify:command-center": "bun test packages/protocol/src && bun --filter '*' typecheck && bun --filter @vulture/desktop-ui build && cargo test -p vulture-core agent && cargo test -p vulture-core workspace && cargo test -p vulture-core storage && cargo test -p vulture-desktop-shell agent_store && cargo test -p vulture-desktop-shell workspace_store && cargo test -p vulture-desktop-shell auth && cargo test -p vulture-desktop-shell state && cargo clippy --workspace --all-targets -- -D warnings",
    "verify": "bun test packages/protocol/src && bun --filter '*' typecheck && bun --filter @vulture/desktop-ui build && cargo test && cargo clippy --workspace -- -D warnings"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^15.0.0",
    "@testing-library/react": "^16.0.0",
    "@vulture/harness-core": "workspace:*",
    "typescript": "^5.8.0"
  }
}
